# Focus v0.1 开发交接

更新时间：2026-08-03

## 下次从这里继续

GitHub 仓库：`BruceHuang-CN/Focus-App`

工作分支：`feature/focus-v0.1-preview`

恢复开发时先执行：

```powershell
git clone https://github.com/BruceHuang-CN/Focus-App.git
cd Focus-App
git fetch origin
git switch feature/focus-v0.1-preview
git pull --ff-only
```

然后阅读本文件，从 **Task 8** 开始。不要重新实现 Task 1–7，也不要直接在 `main` 上续写。

## 代码来源

本分支不是从零创建的空项目。它以用户原有的 `D:\Project\Focusapp` Kotlin/Android 项目为基础继续开发，并保留了原项目当时未提交内容的快照提交。后续所有改动均在独立功能分支完成，没有覆盖原目录。

## 已确认的产品规则

- 目标应用以短视频和信息流应用为主，包括抖音、哔哩哔哩和小红书，并允许用户配置。
- 用户可创建任务，也可为任务设置时间段；当前任务和用户状态共同构成 AI 提醒上下文。
- DeepSeek 等 API 是核心能力，不能移除。当前实现默认使用 DeepSeek V4 Flash，并关闭 thinking 模式。
- 每次任务激活或时间段切换时，AI 一次生成 3 条短提醒并缓存在本地轮换。这不代表只允许提醒 3 次；窗口内次数和每日次数由独立规则控制。
- 切换任务或时间段会更新提醒内容，但不会重置当前统计窗口的已提醒次数。
- 提醒延迟预设为 3/10/30 秒并支持自定义；统计窗口预设为 30/60/120 分钟并支持自定义；窗口内次数预设为 1/3/5 次并支持自定义；每日上限也允许自定义。
- 提醒口吻允许用户选择强度或风格。
- 点击“不刷了”等主动退出操作后，优先返回 Focus，也可选择返回桌面。
- 低内存、低耗电优先：实时模式不使用固定轮询；API 内容提前生成并缓存；统计查询限定时间范围。
- 首页采用已确认的 A 方案：当前任务优先显示。

## 已完成任务

### Task 1：测试基础和可注入时钟

- 增加确定性时钟和测试依赖，避免时间相关测试漂移。
- 提交：`dff4bce`

### Task 2：Room V2 数据结构和迁移

- 增加任务、计划、应用使用会话、提醒事件等 V2 数据结构与 DAO。
- 完成数据库迁移、schema 校验和兼容修复。
- 提交：`671276b`、`8316cef`

### Task 3：自定义设置和滚动提醒规则

- 设置模型升级至 V2。
- 实现提醒延迟、统计窗口、窗口内次数、每日上限和安全范围限制。
- 实现滚动窗口提醒策略，任务切换不会错误重置配额。
- 提交：`00e4154`、`153f9f2`

### Task 4：任务、时间段和当前任务解析

- 实现任务增删改查、完成状态、计划时间段和星期规则。
- 实现当前任务解析与时间边界更新，不使用持续轮询。
- 提交：`66f866a`、`f169292`

### Task 5：目标应用会话和低功耗检测

- 无障碍事件作为推荐实时检测方式。
- UsageStats 作为可选兼容模式，避免两种监控同时运行。
- 记录一次应用打开对应的一次会话，并修复切换和停止时的竞态问题。
- 提交：`72bc2a0`、`5685cf7`、`ee68f13`

### Task 6：可取消提醒与返回操作

- 提醒按会话调度并可取消，离开目标应用后不会继续弹出旧提醒。
- 支持返回 Focus 和返回桌面。
- 增加 Android 15 兼容处理、通知/悬浮权限降级和触发时前台应用复核。
- 提交：`56db0fd`、`abe6df2`

### Task 7：DeepSeek、缓存和 API Key 安全

- 使用 DeepSeek API 一次生成 3 条短提醒并严格解析 JSON。
- 根据任务、状态、提醒口吻和强度生成内容；网络失败时有本地兜底。
- 使用稳定的事务缓存与 LRU 策略，任务激活、状态或 API Key 变化时事件驱动生成。
- 使用 Android Keystore + AES/GCM 保存 API Key，并迁移旧明文设置。
- 修复重复生成、冷启动缓存和并发问题，已通过独立代码审查。
- 提交：`e2de41a`、`3dc0e3d`、`3221bea`、`2025071`

## 未完成任务

### Task 8：设置、首次引导和连接预览（下一个任务）

状态：未开始。为本次 GitHub 交接而在写代码前暂停，没有 Task 8 半成品。

- 添加通用 `PresetSelector`。
- 设置页按目标应用、提醒时间、提醒次数、AI 服务、口吻、返回行为和检测方式分组。
- 提供 3/10/30 秒、30/60/120 分钟、1/3/5 次以及各自的自定义选项。
- 增加 DeepSeek API 真实连接测试：加载、成功并显示模型 ID、简洁错误映射。
- 增加当前任务的口吻预览；无任务时使用本地示例。
- 首次引导准确检测 Focus 的无障碍组件；实时模式为推荐项，UsageStats 为可选兼容项。
- 只在选择兼容前台服务时请求通知权限，并补充相关 ViewModel/UI 测试。

建议提交信息：`feat: add flexible reminder and AI settings`

### Task 9：首页与任务界面

- 首页首先展示当前任务，然后是主要操作、今日短视频摘要和轻量连续达标信息。
- 增加四项底部导航。
- 完成任务列表和编辑器：新建、编辑、完成、删除、设为当前任务、可选时间段和星期选择。
- 时间冲突时就地提示并阻止保存。
- 保留心情/状态快捷入口，作为 AI 上下文。
- 增加 Compose Preview、无障碍语义和 ViewModel/UI 测试。

建议提交信息：`feat: add focus-first task experience`

### Task 10：统计图表和轻量养成系统

- 支持今天、最近 7 天和最近 30 天。
- 展示总时长、打开次数、提醒次数、主动退出次数、继续使用次数和退出率。
- 提供按小时柱状图、按天趋势图、各应用占比环形/饼图。
- 查询只读取所选范围；跨午夜会话按本地日期拆分；缺失时段补零。
- 图表使用 Compose Canvas，处理空数据和单点数据，不使用持续动画。
- 养成规则包括每日短视频限额、当天至少完成一个任务和连续达标天数；主动退出只做正向反馈。

建议提交信息：`feat: add focus statistics and streaks`

### Task 11：端到端验证、性能证据和预览 APK

- 增加完整提醒链路的 fake-boundary 测试，并验证同一次会话不会重复提醒。
- 运行完整单元测试、AndroidTest 编译和 Debug APK 构建。
- 有设备时运行 connected tests；没有设备时必须明确说明，不能声称通过。
- 真机检查抖音、哔哩哔哩、小红书的提醒、主动退出、任务切换、离线兜底和统计一致性。
- 使用 `adb dumpsys meminfo` 与 `batterystats` 记录内存、CPU、唤醒锁、任务、闹钟和网络请求证据。
- 审计仓库，确保没有真实 API Key、第三方强制结束应用 API、旧模型名或 5 秒固定轮询。
- 输出可安装预览 APK 和可复现的验证报告。

建议提交信息：`test: verify focus v0.1 preview`

## 当前验证证据

Task 7 完成后的最近一次完整验证：

- `testDebugUnitTest assembleDebug`：成功。
- 单元测试：68 个，0 失败，0 错误。
- Debug APK：已成功生成于本地构建目录，但 APK 被 `.gitignore` 排除，不随源码分支提交。
- 独立代码审查：通过。
- Android 真机/模拟器 connected tests：尚未执行。
- 真机内存和耗电证据：尚未采集，属于 Task 11。

## 续作约束

- 采用简单、直接、可测试的 Kotlin 写法，避免无必要的抽象和持续后台工作。
- 每个任务先写失败测试，再做最小实现，然后运行任务级测试和完整回归。
- 不要提交 `local.properties`、APK、keystore、API Key、`.gradle`、`.idea` 或构建产物。
- 不要把 DeepSeek API Key 写入源码、日志或 Git 历史。
- 在完成 Task 11 前，不要把当前版本描述为已经通过真机验证或已经达到最终耗电目标。
