# Focus 开发日志

本文件记录开发过程中发现的问题、根因分析与修复，随代码一并提交到仓库。

---

## 2026-08-13（代码已实现，待真机验证）

### 核心 Bug：稍后提醒被短暂前台切换误取消

**现象：**

- 选择 1/5/10 分钟或自定义分钟后，有时不到设定时间就再次提醒，有时到期后完全没有提醒。
- Focus 提醒页、系统弹窗、输入法或通知栏短暂成为前台时，目标 App 会话可能被提前关闭。
- 真机数据库记录表明，部分 WorkManager 稍后提醒在实际执行前已被取消。

**根因：**

1. `AppSessionCoordinator.onPackageChanged()` 收到任何非目标包名后会立即关闭当前会话，并调用同一个 `cancel(sessionId)` 取消首次延迟、稍后提醒、通知和已展示提醒。
2. `FollowUpReminderWorker` 到期时又要求原会话仍打开、仍是当前会话且目标 App 仍在前台。
3. 因此真实离开目标 App 会正确取消提醒，但临时系统窗口或 Focus 自身提醒窗口也可能被误判为真实离开。

**用户确认的产品语义：**

- “稍后提醒”只在用户持续停留于原目标 App 时有效。
- 用户真正离开抖音等目标 App 后，本次稍后提醒立即失效；不发通知，也不弹窗。
- Focus 提醒页、输入法、权限窗口等短暂覆盖不算真正离开。

**已确认方案：**

- 检测到疑似离开时进入短暂待确认状态，1.5 秒后只复核一次真实前台应用。
- 目标 App 已恢复前台则保留原会话和稍后提醒；仍为其他 App 才关闭会话并取消提醒。
- 新前台事件会作废旧的待确认任务，防止旧检查关闭新会话。
- 到期时仍保留现有守护状态、目标配置、会话、真实前台和额度复核。
- 不增加高频轮询或新的常驻服务，控制内存与耗电。

**代码实现：**

- `AppSessionCoordinator` 对疑似离开使用可取消的 1.5 秒单次确认；目标 App 在确认前恢复、前台验证器确认仍在目标 App，都会保留原会话。
- 相同非目标事件不会重复开始倒计时；旧任务通过会话 ID 和事件版本校验，不能关闭新会话。
- 实时无障碍模式使用当前活动窗口包名进行单次复核；兼容模式继续使用 UsageStats。
- 真正离开后仍复用现有取消入口，同时取消首次延迟、WorkManager 稍后提醒、通知和已展示提醒。

**自动化验证：**

- TDD RED：聚焦测试最初因缺少 `scope` 与 `departureConfirmationDelayMillis` 参数而按预期编译失败。
- 聚焦 GREEN：`AppSessionCoordinatorTest` 15 项通过。
- 首次完整回归发现两项早于本轮存在的过时测试：旧版内存稍后提醒断言，以及未注入时钟的返回过渡断言；仅更新测试夹具和预期，没有修改生产行为。
- 最终 `:app:testDebugUnitTest`：43 个测试套件、174 项测试、0 失败、0 错误、0 跳过，`BUILD SUCCESSFUL`。
- 本轮未运行 APK 构建或真机安装；自定义 3 分钟和不同厂商前台窗口行为仍需用户在 Android Studio 集中验证。
---

## 2026-08-10（已确认，待实现）

### 下一版：预设应用组与首页守护开关

- 应用组完全由用户自定义，不提供预置模板；一次仅启用一个组，且启用组不能为空。
- 首次升级会把当前目标 App 迁入“当前应用组”，保留既有检测对象。
- 切换启用组时，结束旧会话并取消待提醒，同步新的检测对象，重置当前提醒额度，并请求 AI 重新生成提醒缓存。
- 设置页新增应用组管理入口：新建、改名、编辑 App、删除与启用；最后一个组不可删除。
- 首页新增“启动守护”开关。关闭后暂停 Focus 的检测与提醒，但不关闭系统无障碍服务；重新开启后按当前检测模式和当前应用组恢复。
- 不新增后台常驻服务、轮询或网络请求；历史使用统计不因切换组或暂停守护而被删除。

### 已确认后续任务（单独设计与实现）

- **反馈入口**：不做应用内文字反馈或自建后端；在设置页放置可替换的问卷星二维码，并预留微信公众号二维码/说明位置。
- **赞助入口**：在设置页提供独立赞助卡片，展示可替换的收款二维码与说明；不接入支付 SDK，不处理支付结果。
- 上述两个静态入口与应用组功能解耦，待应用组真机验证完成后再分别确定文案、二维码资源与页面细节。

---

## 2026-08-09

### 核心 Bug 修复：离开目标 App 后仍弹提醒、仍占用额度

- 提醒额度的数据库更新现在带有 `endedAt IS NULL` 条件。会话若在提醒写入前结束，写入会失败，因此不会展示提醒，也不会占用本时间窗额度。
- 会话结束时，调度器会同时取消等待任务、取消同会话通知，并向已打开的提醒页发送关闭信号。
- 提醒页在展示前和恢复到前台时都会核验该会话仍是当前未结束会话；旧通知或延迟事件无法重新打开过期提醒。
- 本轮没有调用 `disableSelf()`，不会由应用代码关闭系统无障碍服务；使用记录链路保持不变。

### 核心 Bug 修复：返回指定 App 失败却被当作成功

- 指定 App 跳转改为返回明确结果：未选择应用、应用不可用、系统拒绝启动或启动成功。
- 只有成功启动指定 App 后，才记录「已返回指定应用」并关闭提醒；失败时保留提醒页，显示可理解的失败原因，用户可重新选择或重试。

### 验证与构建环境

- 新增并通过会话结束竞争、已展示提醒关闭、指定 App 启动失败三个回归测试。
- `testDebugUnitTest` 全量通过。
- 修正两个 Android Compose 测试使用了当前依赖中不存在断言 API 的导入；`compileDebugAndroidTestKotlin` 与 `assembleDebug` 现可通过。
- Gradle 守护进程工具链由 21 调整为 17；项目源码目标本身为 Java 11，当前 Android Gradle Plugin 支持 Java 17，可避免本机在没有 JDK 21 时无法运行测试。

---

## 2026-08-09（第二轮）

### Bug 修复：提醒弹窗闪退、Focus 不出现在最近任务

- 根因是实时检测把 `ReminderActivity` 自己的窗口事件当作「用户离开目标 App」，会话协调器因此取消会话，并立刻关闭刚展示的提醒。
- 实时模式现在按 `ReminderActivity` 精确识别该事件；兼容模式在提醒页可见期间使用内存状态识别该事件。两种模式都只忽略提醒页自身的 Focus 事件，用户手动回到 Focus、桌面或其他 App 时仍会正常结束会话并记录使用情况。
- 这避免提醒页被自己的关闭信号闪退；提醒页本身仍不出现在最近任务，这是它作为临时提醒页的既有配置，普通 Focus 首页不受该配置影响。

### 交互修复：设置页退出确认

- 点击顶部返回或系统返回键时，若本页改过设置，会显示「保存并退出 / 继续修改」。
- 系统返回由导航层在 `NavHost` 后统一转交给设置页，避免底部导航优先消费返回事件。API Key 仍由单独的「保存 Key」按钮立即安全保存。

### 验证

- `testDebugUnitTest`、`compileDebugAndroidTestKotlin`、`assembleDebug` 均通过。
- 真机检查：打开目标 App 等待提醒，确认提醒持续显示；点击「返回 Focus」后确认会话结束；改动提醒时间后分别测试保存退出和放弃修改。

---

## 2026-08-08（修复）

### 问题：首页统计/提醒记录为空、统计图表不显示、文字看不清

**现象**：首页打开次数、主动退出、提醒记录全部为 0/空；统计页只有打开次数有值，
三个图表显示"暂无数据"；统计与首页文字颜色与背景太接近看不清。

**根因**：

1. 首页仍从旧的 `app_usage_events` 表读取统计与提醒记录，而检测流程已全面改用
   `app_usage_sessions` 表，旧表无人写入 → 数据恒为 0/空。
2. 图表时长按分钟向下取整，测试时打开应用只有几秒 → 0 分钟 → 图表无数据。
3. 全局次要文字色 `outline` 过浅（#D8D2C3），与背景对比度不足。

**修复**：

- 首页统计与最近提醒记录改为读取会话表，与统计页数据源一致。
- 统计时长改为向上取整（至少 1 分钟），短时打开也能出图。
- 全局次要文字颜色调深（outline = #6F6A5F），提升可读性。

---

## 2026-08-08

### 功能与修复

- **目标 App 列表看不到部分应用**：Android 11+ 包可见性限制导致应用列表被过滤。
  在 AndroidManifest 增加 `<queries>` 声明（MAIN/LAUNCHER intent），
  使抖音、小红书、哔哩哔哩等带启动入口的应用可被列出。
- **呼吸停顿开关找回**：设置页「提醒时间」重新加入「提醒前呼吸停顿」开关，
  由用户自行选择是否在弹窗前先深呼吸（之前重构设置页时该开关被遗漏）。
- **返回行为支持指定应用**：返回行为新增「返回指定应用」选项，填写目标包名后，
  提醒弹窗的主动退出按钮会跳转到该应用；包名无效时回退到桌面。
- **返回指定应用改为按名称选择**：不再要求用户输入包名，改为从已安装应用列表
  按应用名称选择（与目标 App 选择同一套交互），包名在内部自动保存。

---

## 2026-08-07（补充）

### 功能：提醒额度可视化、额度重置与半手动保存

**背景**：窗口内提醒额度用尽时提醒被静默跳过，用户不知道原因；
设置页每次改动都弹"已自动保存"，缺少操作感。

**实现**：

- 提醒弹窗新增一行"本窗口（X 分钟）已提醒 X/Y 次"，展示当前额度使用情况。
- 设置页「提醒次数」分组新增「重置当前窗口额度并重新生成提醒」按钮：
  清空当前窗口内的已提醒标记，并通过缓存刷新信号让 AI 在后台重新生成目标 App 的提醒文案。
- 设置页改为半手动保存（B 方案）：改动仍即时保存，但不再弹"已自动保存"提示，
  顶部显示"有未保存的修改"状态与「保存设置」按钮，提供确认操作感。

---

## 2026-08-07

### 功能：设置页「检测状态」权限检查窗口

**背景**：实时模式不触发提醒时，应用没有任何反馈，用户无法判断是哪一环权限缺失。

**实现**：

- 设置页顶部新增可展开的「检测状态」窗口，按当前检测方式列出所需权限检查项，
  每项显示 ✓/✗ 与「去开启」入口，点击跳转对应的系统设置或应用内开关。
- 实时模式检查：系统无障碍服务、应用内「启用无障碍检测」、通知权限、
  显示在其他应用上层、目标 App。
- 兼容模式检查：使用情况访问权限、通知权限、显示在其他应用上层、目标 App。
- 从系统设置返回时自动刷新检查结果。
- 检查逻辑为纯函数 `PermissionCheckEvaluator`，配套单元测试与 UI 测试。

---

## 2026-08-06

### 问题 1：提醒弹窗不出现（真机实测）

**现象**：安装后页面显示正常，但打开目标 App 后没有任何提醒弹窗；首次打开 App 时也没有权限提示。

**根因分析**（按调用链逐层排查）：

1. 引导页选择检测方式后，只跳转系统设置页并标记引导完成，**没有把所选模式写入应用设置**。
   `enableAccessibility` 默认值为 `false`，且唯一的写入点是设置页的「启用无障碍检测」开关。
2. `FocusAccessibilityService.shouldProcessAccessibilityEvents()` 要求
   `detectionMode == REALTIME && enableAccessibility` 同时成立；开关为 false 时，
   无障碍事件在入口被直接丢弃 → 不产生会话 → 不触发提醒。
3. 兼容模式同理：`MainActivity` 仅在 `detectionMode == COMPATIBILITY` 时启动检测服务，
   引导页同样没有写入该值，服务不会启动。
4. 「首次打开无权限提示」符合当前设计（应用不主动请求权限，仅由引导按钮跳系统页）；
   一旦完成过引导（SharedPreferences 标记），之后直接进入首页。

**修复**：

- 引导页选择检测方式时即调用 `SettingsViewModel.applyOnboardingDetectionMode(mode)`，
  持久化 `detectionMode` 与 `enableAccessibility`。
- 设置页监听生命周期：实时模式下若系统无障碍已开启，自动把应用内开关置为开启
  （`ensureAccessibilityEnabled()`），即使之前没手动开开关也能恢复检测。
- 「启用无障碍检测」开关与实时模式选中时，未开启则跳转系统无障碍设置页。
- 引导页进入 AI 配置步骤时（Android 13+）请求通知权限，实时模式同样覆盖。

### 问题 2：弹窗可能被静默吞掉（悬浮窗与通知权限）

**现象**：即使检测链路通了，也可能看不到任何提醒。

**根因分析**：

1. 提醒展示依赖「显示在其他应用上层」（SYSTEM_ALERT_WINDOW），
   无权限时降级为通知；但应用内**没有任何地方引导用户开启悬浮窗权限**。
2. Android 13+ 通知需要运行时权限，实时模式路径此前不会请求通知权限；
   通知被拒绝时降级通知也会被系统丢弃。

**修复**：

- `PermissionHelper` 增加悬浮窗权限检测与跳转（`ACTION_MANAGE_OVERLAY_PERMISSION`）。
- 设置页「检测方式」分组新增悬浮窗权限状态行与「去开启」入口，并在返回页面时刷新状态。
- 切换到任一检测方式时都会请求通知权限（Android 13+）。

**验证方式**：Android Studio 构建后真机复测——目标 App 打开后停留超过提醒延迟，
应出现悬浮窗弹窗或通知；首次引导完成后检查设置中的检测方式与无障碍开关状态。

---

## 2026-08-08

### 功能：应用启动图标（Ant Design 精选）

**背景**：App 此前使用 Android Studio 默认机器人图标，用户希望更换为有辨识度的启动图标。
先制作了网页预览页（`outputs/icon-preview.html` 与 `outputs/antd-icon-preview.html`），
从 Ant Design 官方图标库挑选 20 个热门图标，支持「线性/填充/双色 × 4 套配色 × 3 种形状 × 日间/夜间」切换预览。

**用户选定方案**：薄荷青渐变 + 线性风格 + 圆角，共 5 个候选图形：

- 沙漏 Hourglass（当前默认，即 `ic_launcher`）
- 靶心 Aim（`ic_launcher_aim`）
- 铃铛 Bell（`ic_launcher_bell`）
- 历史 History（`ic_launcher_history`）
- 时钟 ClockCircle（`ic_launcher_clockcircle`）

**实现**：

- 使用官方 Ant Design SVG 路径，脚本（`outputs/generate-icons.js`）批量渲染：
  - 旧系统（API < 26）回退图标：各密度 `ic_launcher_*.webp`（48/72/96/144/192px，薄荷青渐变圆角底 + 白色线性图形）。
  - 自适应图标（API 26+）：`mipmap-anydpi-v26/ic_launcher_*.xml` + 各密度前景层 `*_foreground.webp`
    （108dp 画布、透明背景、图形居中于安全区），背景层复用 `drawable/ic_launcher_background.xml`（315° 渐变）。
- 默认使用沙漏（`@mipmap/ic_launcher`，Manifest 无需改动）；其余四套候选资源已生成。

**切换图标方法**：把 `app/src/main/AndroidManifest.xml` 中
`android:icon` / `android:roundIcon` 改成对应资源名即可，
例如 `@mipmap/ic_launcher_aim`（圆角图标同步改 `@mipmap/ic_launcher_aim_round`）。

**验证方式**：Android Studio 构建后安装到手机，桌面与最近任务栏查看图标效果；对比 5 个候选后告知取舍。

---

## 2026-08-08（第二轮）

### 功能：应用内图标展示

- 新增 `drawable-nodpi/ic_app_logo.webp`（薄荷青圆角沙漏，192px），首页顶栏左侧显示应用图标，打开 App 即可直观看到新图标。

### 功能：外观主题（日间/夜间 + 4 套配色）

- 新增 `AppThemeMode`（跟随系统/日间/夜间）与 `AppThemeColor`（薄荷青/宁静蓝/暖阳橙/石墨）模型。
- 新增 `ThemeStore`（SharedPreferences 持久化，默认：跟随系统 + 薄荷青），与设置仓库解耦，无需数据库迁移。
- `FocusAppTheme` 按主题模式与配色生成 Material3 明/暗色板，`MainActivity` 全局应用，切换即时生效。
- 设置页新增「外观主题」分区：界面模式选择（跟随系统/日间/夜间）+ 四套配色圆点选择，改动实时预览，无需手动保存。

### 问题：退出目标应用后，打开 Focus 仍弹出「继续专注」提醒

**现象**：退出所选专注应用并进入 Focus 后，不久又弹出提醒弹窗。
**根因**：提醒由后台检测服务的调度器触发，属于后台链路，但问题不在“后台没清干净”，而在于提醒触发条件不完整：

1. 点击「仍要使用」后安排的跟进提醒（`scheduleFollowUp`）只检查额度，**没有检查会话是否仍在进行**；
2. 初始提醒在延迟后仅依赖前台校验，存在检测滞后窗口（兼容模式轮询 10 秒/使用统计事件延迟）时可能误弹。

**修复**：

- 跟进提醒与初始提醒在展示前都校验 `currentOpenSession()?.id == session.id`（会话已关闭即不弹）；
- 跟进提醒额外校验 `session.endedAt == null`。

**验证**：新增 2 个回归测试（会话关闭后初始/跟进提醒均不展示），`ReminderSchedulerTest` 8 项全部通过。

### 测试修复（既存问题）

- 补齐 `OpenAiApi.listModels`、`AppSessionRepository` 抽象方法缺失导致的测试编译失败（`DeepSeekReminderProviderTest`、`AiRepositoryTest`、`GetStatsUseCaseTest`、`StreakStatusUseCaseTest`、`SettingsViewModel*Test`）。
- 修正 `StreakStatusUseCaseTest` 连续天数测试闭包计数（把「今日已完成」查询计入一次调用导致期望值偏差）。
- 单元测试全量 120 项通过。

---

## 2026-08-11

### 应用组管理界面

- 设置页提供应用组入口，以及应用组列表和编辑器。
- 应用组不可为空；支持创建、编辑和启用；仅剩最后一组时受删除保护。
- 启用继续使用既有的安全切换用例；创建或删除应用组不会使当前应用组与守护目标脱节。
- 首页提供守护状态和额度重置。本条不表示已完成真机测试、反馈二维码、赞助、强制选择设置或其他路线图工作。

**验证：** `$env:GRADLE_USER_HOME='C:\Users\6\.gradle'; .\gradlew.bat :app:testDebugUnitTest :app:compileDebugAndroidTestKotlin :app:assembleDebug --no-daemon --no-configuration-cache`，结果：`BUILD SUCCESSFUL in 24s`。

## 2026-08-08（第三轮）

### 问题：release 构建报 lint fatal error（RemoveWorkManagerInitializer）

**现象**：`assembleRelease` 失败，AndroidManifest.xml 报
`Remove androidx.work.WorkManagerInitializer ... when using on-demand initialization`。
**原因**：`FocusApp` 实现了 `androidx.work.Configuration.Provider`（按需初始化，配合 HiltWorkerFactory），
但 WorkManager 通过 androidx.startup 在 Manifest 中自动注册了默认初始化器，两种初始化方式冲突，lint 判定为致命错误。
**修复**：在 Manifest 中移除 WorkManager 默认初始化器（`tools:node="remove"` 移除
`androidx.work.WorkManagerInitializer` 的 meta-data），遵循 WorkManager 官方按需初始化文档。
**验证**：`:app:assembleRelease --offline` 构建成功，lintVitalRelease 通过，输出
`app/build/outputs/apk/release/app-release-unsigned.apk`（未签名，需在 Android Studio 签名向导中导出正式包）。

---
